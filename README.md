# Plotz-Size-Calculator-React.js-
Plotz Size Calculator is a responsive web app built with React.js that allows users to calculate plot sizes for various shapes (Rectangle, Circle, Triangle, etc.) in units like square feet, gaz, and square meters. It features dynamic inputs, a reset button, and custom CSS for a clean and modern design.
